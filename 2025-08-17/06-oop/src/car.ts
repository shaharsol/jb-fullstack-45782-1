class Car {
    // member variables (e.g. properties)
    make: string
    model: string
    engineVolume: number
    yearModel: number

    // methods (e.g. function)
    ignite() { 
        console.log('hello')
    }
}